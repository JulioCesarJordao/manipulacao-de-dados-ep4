# GamePower Database Project

Este repositório contém os scripts SQL do banco de dados **GamePower**, criado para a Experiência Prática 4.

## 📌 Estrutura do Repositório

```
gamepower-db/
│── 01_ddl.sql               # Criação das tabelas (DDL)
│── 02_inserts.sql           # Inserção de dados (INSERT)
│── 03_selects.sql           # Consultas SELECT (JOIN, WHERE, ORDER BY)
│── 04_updates_deletes.sql   # Atualizações e exclusões (UPDATE/DELETE)
│── README.md
```

## 🚀 Como Executar

1. Instale o **MySQL Server** e **MySQL Workbench**.
2. Abra o Workbench e crie uma conexão local.
3. Execute o script `01_ddl.sql` para criar o schema e as tabelas.
4. Execute `02_inserts.sql` para povoar o banco.
5. Execute as consultas em `03_selects.sql`.
6. Teste os comandos de `04_updates_deletes.sql`.

## 🗄️ Requisitos

- MySQL Server 8.x
- MySQL Workbench
